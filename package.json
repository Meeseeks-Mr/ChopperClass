{
  "name": "chopper-class",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":     "vite",
    "build":   "vite build",
    "preview": "vite preview --host",
    "deploy":  "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "0.160.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.4.21"
  }
}
